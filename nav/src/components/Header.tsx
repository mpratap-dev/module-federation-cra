import React from "react";

const Header: React.FC = () => (
  <div>
    <header style={{ background: '#fff', color: '#1b78bf', padding: 20 }}>MODULE FEDERATION</header>
  </div>
);

export default Header;